# Open Omarchy 内核参数调优 (配合 ZRAM 使用)
# 放置为 /etc/sysctl.d/99-openomarchy.conf
vm.swappiness = 180            # 优先使用 ZRAM 压缩交换 (zram 场景推荐 >100)
vm.vfs_cache_pressure = 50     # 保留更多 dentry/inode 缓存
vm.page-cluster = 0            # zram 随机访问场景关闭预读
